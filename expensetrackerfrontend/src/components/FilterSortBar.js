import React from 'react';

const FilterSortBar = ({ type }) => {
  // Placeholder for filter/sort UI
  return (
    <div style={{ margin: '10px 0' }}>
      <label>Filter/Sort ({type}): </label>
      {/* Add filter/sort controls here */}
    </div>
  );
};

export default FilterSortBar;
